/**
 * Feature flags — keep related code paths in the codebase; toggle when ready.
 * Tax deduction / fiscal receipt UI and server-side PDF sends: off until compliance.
 */
export const FEATURE_TAX_DEDUCTION_UI = false;
